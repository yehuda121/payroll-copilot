/**
 * Single source of truth for the product brand name.
 * UI copy that is not locale-specific should import from here.
 * Locale files keep `common.appName` in sync for translated chrome that still uses i18n.
 */
export const APP_NAME = 'Payroll Copilot';

/** Short mark for compact logo badges (not a translation key). */
export const APP_NAME_SHORT = 'PC';
