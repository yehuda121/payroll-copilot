import { ValidationWizard } from '../../features/guest/validation/ValidationWizard';
import '../../features/guest/guest.css';

/**
 * Guest document upload area on the public landing page.
 */
export function GuestUploadArea() {
  return <ValidationWizard />;
}
